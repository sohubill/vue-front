version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10
    branches:
      only:
        - master
    steps:
        # - add_ssh_keys:
        #   fingerprints:
        #     - ''
        # - checkout
      - run:
          name: 'print'
          command: echo "a first hello"
      - run:
          name: 'node version'
          command: npm -v

