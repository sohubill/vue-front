version: '2'
jobs:
  build:
    docker:
      - image: circleci/node:10
    branches:
      only:
        - master
      steps:
        # - add_ssh_keys:
        #   fingerprints:
        #     - ''
        # - checkout
        - run: echo "a first hello"
        - run: npm -v 

